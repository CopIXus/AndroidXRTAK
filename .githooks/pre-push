#!/bin/sh
if command -v pwsh >/dev/null 2>&1; then
  pwsh -NoProfile -File "$(dirname "$0")/../ci/verify-no-secrets.ps1" || exit 1
else
  powershell -NoProfile -File "$(dirname "$0")/../ci/verify-no-secrets.ps1" || exit 1
fi